[pytest]
testpaths = tests
python_files = test_*.py
markers =
    tier1: pure Python validation
    tier2: AiiDA integration validation
    tier3: real backend validation
    requires_aiida: requires an AiiDA profile
