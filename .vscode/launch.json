{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File (with verdi restart)",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "preLaunchTask": "verdi daemon restart"
    }
  ]
}
